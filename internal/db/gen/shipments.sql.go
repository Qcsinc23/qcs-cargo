// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: shipments.sql

package gen

import (
	"context"
)

const getShipmentByID = `-- name: GetShipmentByID :one
SELECT s.id, s.destination_id, s.manifest_id, s.ship_request_id, s.tracking_number, s.status,
       s.total_weight, s.package_count, s.carrier, s.estimated_delivery, s.actual_delivery,
       s.created_at, s.updated_at
FROM shipments s
INNER JOIN ship_requests sr ON s.ship_request_id = sr.id
WHERE s.id = ? AND sr.user_id = ?
`

type GetShipmentByIDParams struct {
	ID     string `json:"id"`
	UserID string `json:"user_id"`
}

func (q *Queries) GetShipmentByID(ctx context.Context, arg GetShipmentByIDParams) (Shipment, error) {
	row := q.db.QueryRowContext(ctx, getShipmentByID, arg.ID, arg.UserID)
	var i Shipment
	err := row.Scan(
		&i.ID,
		&i.DestinationID,
		&i.ManifestID,
		&i.ShipRequestID,
		&i.TrackingNumber,
		&i.Status,
		&i.TotalWeight,
		&i.PackageCount,
		&i.Carrier,
		&i.EstimatedDelivery,
		&i.ActualDelivery,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listShipmentsByUser = `-- name: ListShipmentsByUser :many
SELECT s.id, s.destination_id, s.manifest_id, s.ship_request_id, s.tracking_number, s.status,
       s.total_weight, s.package_count, s.carrier, s.estimated_delivery, s.actual_delivery,
       s.created_at, s.updated_at
FROM shipments s
INNER JOIN ship_requests sr ON s.ship_request_id = sr.id
WHERE sr.user_id = ?
ORDER BY s.created_at DESC
`

func (q *Queries) ListShipmentsByUser(ctx context.Context, userID string) ([]Shipment, error) {
	rows, err := q.db.QueryContext(ctx, listShipmentsByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Shipment
	for rows.Next() {
		var i Shipment
		if err := rows.Scan(
			&i.ID,
			&i.DestinationID,
			&i.ManifestID,
			&i.ShipRequestID,
			&i.TrackingNumber,
			&i.Status,
			&i.TotalWeight,
			&i.PackageCount,
			&i.Carrier,
			&i.EstimatedDelivery,
			&i.ActualDelivery,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
